import React from 'react';

/**
 * ClientTaskTile
 * Custom metadata injected into the master TaskCard's 2nd Row.
 */
const ClientTaskTile = ({ task }) => {
  // If we have client-specific metadata to show as a badge, it goes here.
  // For now, we return null until client badges are needed.
  return null;
};

export default ClientTaskTile;
